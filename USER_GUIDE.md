Read AGENTS.md sections 6.1–6.8 before starting. Do not build the downloader yet. Environment: ThetaData v3. Two options for access — evaluate both and tell me which you recommend for a multi-hour batch download: (a) Theta Terminal v3, local REST at http://127.0.0.1:25503/v3 (b) the thetadata Python library (pip install thetadata, Python 3.12+), which needs no Terminal and returns Polars/Pandas directly Do not hard-code the base URL — read it from config/default.yaml. Verify and print: 1. Both subscriptions active: Options Value AND Index Value. Print the access level and first-access date for each data type. 2. One call to each, printing the RAW response and exact field names: - option/history/quote (SPXW, 2026-09-17, strike_range=2, max_dte=0, right=both, interval=1m) - index/history/price (SPX, 2026-09-17, interval=1m) - index/history/price (SPX, 2026-09-17, interval=1s) - index/history/eod (SPX, 2026-09-17) 3. CRITICAL: confirm whether interval=1s is permitted on our Index Value tier, or whether it returns a permission error. Report the exact error if it fails. 4. For the 1s pull: row count, and how many distinct timestamps appear. The docs say repeated prices are omitted, so report the gap distribution. 5. For index/history/eod: the close value and the timestamp of the final print. Confirm whether it lands after 16:00:00 ET. Stop after printing. Do not build the downloader.
